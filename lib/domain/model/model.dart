class SliderObject {
  String title;
  String subtitle;
  String image;

  SliderObject(this.title, this.subtitle, this.image);
}

class User {
  int id;
  String nome;
  String email;
  String password;
  int age;

  User(this.id, this.nome, this.email, this.password, this.age);
}

class DeviceInfo {
  String name;
  String identifier;
  String version;
  DeviceInfo(this.name, this.identifier, this.version);
}
